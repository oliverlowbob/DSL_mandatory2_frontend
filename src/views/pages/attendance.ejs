<!DOCTYPE html>
<html lang=en>

<head>
    <%- include('../partials/head'); %>
</head>

<body>
    <header>
        <%- include('../partials/header'); %>
    </header>
    <main class="container">
        <h3>Attendance overview</h3>
        <table class="striped">
            <thead>
                <tr>
                    <th>Firstname</th>
                    <th>Lastname</th>
                    <th>E-mail</th>
                    <th>Key</th>
                    <th>Present</th>
                </tr>
            </thead>

            <tbody>
                <% students.forEach(student => { %>
                <tr>
                    <td><%= student.firstName %></td>
                    <td><%= student.lastName %></td>
                    <td><%= student.email %></td>
                    <td><%= student.key %></td>
                    <td id="<%= student.key %>"><%= student.present %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </main>

    <%- include('../partials/footer'); %>
</body>

</html>